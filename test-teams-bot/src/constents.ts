export enum EventIds {
    POST_PUBLISHED = 'post.published',
    MEMBER_VERIFIED = 'member.verified',
    MEMBER_INVITATION_CREATED = 'member_invitation.created',
    SPACE_JOIN_REQUEST_ACCEPTED = 'space_join_request.accepted',
    SPACE_JOIN_REQUEST_CREATED = 'space_join_request.created',
    MODERATION_CREATED = 'moderation.created',
    MODERATION_REJECTED = 'moderation.rejected',
    MODERATION_ACCEPTED = 'moderation.accepted',
    SPACE_MEMBERSHIP_DELETED = 'space_membership.deleted',
    SPACE_MEMBERSHIP_CREATED = 'space_membership.created',
    WELCOME = 'welcome',
}
